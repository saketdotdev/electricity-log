name: log-workflow

on:
  push:
    branches:
      - main

jobs:
  consumption-logged:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: log consumption
        run: echo "electricity consumption logged"
